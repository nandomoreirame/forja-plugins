{
  "name": "forja-plugins",
  "version": "1.0.7",
  "private": true,
  "description": "Official plugin registry for Forja",
  "author": "Forja Team",
  "license": "MIT",
  "scripts": {
    "build:registry": "node scripts/build-registry.mjs",
    "validate": "node scripts/validate-plugins.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
